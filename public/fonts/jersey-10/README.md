# Jersey 10 local font assets

Place one of the following files in this folder to enable the in-game font without external network access:

- `Jersey10-Regular.woff2` (preferred)
- `Jersey10-Regular.ttf`

The app already references these files in `src/shared/styles/global.css` via `@font-face`.
